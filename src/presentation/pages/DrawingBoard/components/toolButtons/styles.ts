import styled from 'styled-components';

const ToolBtn = styled.div`
  position: absolute;
  top: 10px;
  left: 10px;
  z-index: 1;
  display: flex;
`;

const Button = styled.button`

`;

export const Styled = {
  ToolBtn,
  Button
};